import pyray


class Scene:

    def construct(self):
        Warning("scene.py:draw must be implemented by the child class.")

    def draw(self):
        Warning("scene.py:draw must be implemented by the child class.")

    def update(self):
        Warning("scene.py:draw must be implemented by the child class.")
